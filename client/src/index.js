import React from 'react';
import ReactDOM from 'react-dom';
// not using redux store yet
// import { Provider } from 'react-redux';

import App from './components/App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
